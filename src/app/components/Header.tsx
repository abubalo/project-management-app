import React from "react";

type Props = {};

const Header = (props: Props) => {
  return (
    <header className="p-4 flex justify-between items-center bg-forground">header</header>
  );
};

export default Header;
